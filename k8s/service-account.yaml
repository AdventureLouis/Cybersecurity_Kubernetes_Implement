apiVersion: v1
kind: ServiceAccount
metadata:
  name: threat-detection-sa
  namespace: threat-detection
  annotations:
    # This will be replaced by the deploy script with the actual role ARN
    eks.amazonaws.com/role-arn: "${EKS_POD_ROLE_ARN}"
  labels:
    app: threat-detection
